# Secrets

This folder is for local secret references only.

Do not commit API keys, tokens, passwords, OAuth files, or service credentials.

Prefer `.env` for Phase 1 local development, and keep `.env` ignored by Git.
