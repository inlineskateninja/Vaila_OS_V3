# System Rules

- Vaila OS V3 prioritizes stable local operation.
- The CLI must work before the desktop client or local API is treated as complete.
- The system must not directly modify core files during self-assessment.
- Proposed changes must be exported to `Sandbox/Proposed_Patches/`.
- Sensitive memory and logs must not be committed to GitHub.
