# Connected Services Registry

This folder stores metadata about services Vaila can connect to.

Do not store secrets here.

Credentials belong in local environment variables, encrypted stores, or ignored files under `Secrets/`.
